# Core
python-dateutil>=2.8.2
pyyaml>=6.0
pydantic>=2.6
python-dotenv>=1.0.1
click>=8.1.7
tqdm>=4.66.1

# Data processing
polars>=0.20.0
pandas>=2.1.0
numpy>=1.26
pyarrow>=14.0.1

# Schema validation (data quality)
pandera>=0.18.0

# Dataset sources
kaggle>=1.6.0
requests>=2.31

# Streaming (for later phases)
aiokafka>=0.10.0
# confluent-kafka>=2.3.0     # uncomment when running real Kafka
# apache-flink (separate JVM)

# ML (training - install in a separate venv if heavy)
# scikit-learn>=1.4.0
# xgboost>=2.0
# lightgbm>=4.3
# onnx>=1.15
# onnxruntime>=1.16
# torch>=2.2
# torch-geometric>=2.5

# Dev / Test
pytest>=8.0
pytest-cov>=4.1
black>=24.0
ruff>=0.3
fakeredis>=2.20

# --- Thêm cho Giai đoạn 1 & API (Local Architecture) ---
fastapi>=0.110.0
uvloop>=0.19.0
orjson>=3.9.15
redis[hiredis]>=5.0.0
simpleeval>=0.9.13
uvicorn[standard]>=0.27.0
duckdb>=0.10.0
quixstreams>=2.5.0

